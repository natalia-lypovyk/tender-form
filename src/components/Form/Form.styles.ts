import styled from 'styled-components';

const Form = styled.form`
  width: 100%;
  max-width: 600px;
  padding: 10px;
  border: 1.5px solid var(--c-positive-light);
  border-radius: 8px;
`;

export default { Form };
